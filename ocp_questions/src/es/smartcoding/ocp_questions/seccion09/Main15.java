/**
 * 
 */
package es.smartcoding.ocp_questions.seccion09;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;

/**
 * @author jmendez
 *
 */
public class Main15 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
//		Path path = Paths.get("/temporal");
//		boolean presente = Files.walk(path).filter((p, a) -> a.isDirectory() && !path.equals(p)) // (1)
//				.findFirst().isPresent(); // (2)
//		System.out.println(presente ? "Directorio vacío" : "Directorio tiene subdirectorios");
	}

}
